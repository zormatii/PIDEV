<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Liste des Collaborations</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid black; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
    </style>
</head>
<body>
    <h2>Liste des Collaborations</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Type</th>
                <th>Date Signature</th>
                <th>Date Expiration</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for collaboration in collaborations %}
                <tr>
                    <td>{{ collaboration.id }}</td>
                    <td>{{ collaboration.nomC }}</td>
                    <td>{{ collaboration.type }}</td>
                    <td>{{ collaboration.dateSig|date('Y-m-d') }}</td>
                    <td>{{ collaboration.dateEx|date('Y-m-d') }}</td>
                    <td>{{ collaboration.status }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
